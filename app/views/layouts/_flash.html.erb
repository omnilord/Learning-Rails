<% if flash.any? %>
  <div class="row flash">
  <% flash.each do |n, m| %>
    <div class="col-xs-12 label label-<%= n %> flash-msg" align="center">
      <%= html_escape(m) %>
    </div>
  <% end %>
  </div>
<% end %>
