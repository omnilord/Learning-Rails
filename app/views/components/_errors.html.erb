<%
if obj.errors.any?
  errors = obj.errors.full_messages.uniq
%>
<div class="container">
  <div class="col-xs-8 col-xs-offset-2">
    <div class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title error">
          The following <%= pluralize(errors.count, 'error') %> occurred during save:
        </h3>
      </div>
      <div class="panel-body">
        <% errors.each do |msg| %>
        <ul>
          <li><%= msg.html_safe %></li>
        </ul>
        <% end %>
      </div>
    </div>
  </div>
</div>
<% end %>
